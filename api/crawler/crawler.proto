syntax = "proto3";
package crawler;

import "api/song/song.proto";

service Crawler { rpc Crawl(CrawlRequest) returns (CrawlResponse); }

message CrawlRequest {
  string source = 1;
  repeated string urls = 2;
}

message CrawlResponse {
  bool success = 1;
  repeated song.Song data = 2;
}